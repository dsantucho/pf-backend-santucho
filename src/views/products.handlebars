{{!-- VA POR HTTP--}}
<div class="container my-8">
    <div class=" m-16 bg-slate-950 text-white p-4 rounded-md">
        <h1 class="text-2xl tracking-wide font-sans">Bienvenido, {{username}}!</h1>
        <p class="text-2xl tracking-wide font-sans">Rol: {{rol}}</p>
        <div class="mt-4">
            <a class="font-bold py-2 px-4 rounded text-white bg-red-600" href="/auth/logout">Logout</a>
        </div>
        
    </div>
    <h1 class="  size-20 w-full text-4xl tracking-wide text-center underline uppercase font-sans">View Products </h1>
    <h2 class="w-full text-2xl tracking-wide text-center  font-sans  underline">Lista de Productos</h2>
    <div id = "pages" class="mt-5 flex flex-wrap justify-center"></div>
    <div id="productList" class="mt-5 flex flex-wrap justify-center"></div>

    <!-- Botones de paginación -->
    <div id ="pagination" class="flex justify-center mt-5">
     </div>
</div>

<script src="/js/index.js"></script>